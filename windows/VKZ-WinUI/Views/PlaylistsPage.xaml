<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="VKZ.Views.PlaylistsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:VKZ.Views"
    xmlns:models="using:VKZ.Models"
    xmlns:converters="using:VKZ.Converters"
    Background="{StaticResource BackgroundColor}">

    <Page.Resources>
        <converters:BoolToVisibilityConverter x:Key="BoolToVisibility" />
        <converters:NumberFormatConverter x:Key="NumberFormat" />
    </Page.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Заголовок -->
        <StackPanel Grid.Row="0" Padding="24,16,24,8">
            <TextBlock
                Text="Мои плейлисты"
                Style="{StaticResource TitleTextStyle}" />
            <TextBlock
                x:Name="UserInfoText"
                Style="{StaticResource CaptionTextStyle}"
                Margin="0,4,0,0" />
        </StackPanel>

        <!-- Список плейлистов -->
        <ListView
            x:Name="PlaylistsListView"
            Grid.Row="1"
            Background="Transparent"
            SelectionMode="Single"
            ItemTemplate="{StaticResource PlaylistTemplate}"
            ContainerContentChanging="OnContainerContentChanging"
            Margin="12,0">
            <ListView.ItemContainerStyle>
                <Style TargetType="ListViewItem">
                    <Setter Property="Background" Value="Transparent" />
                    <Setter Property="Foreground" Value="{StaticResource TextPrimaryColor}" />
                    <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                    <Setter Property="Padding" Value="8,4" />
                    <Setter Property="CornerRadius" Value="8" />
                    <Setter Property="BorderThickness" Value="0" />
                    <Setter Property="Margin" Value="0,2" />
                </Style>
            </ListView.ItemContainerStyle>
        </ListView>

        <!-- Индикатор загрузки -->
        <ProgressRing
            x:Name="LoadingRing"
            IsActive="False"
            Visibility="Collapsed"
            HorizontalAlignment="Center"
            VerticalAlignment="Center"
            Width="40"
            Height="40"
            Foreground="{StaticResource AccentColorBrush}" />

        <!-- Пустое состояние -->
        <TextBlock
            x:Name="EmptyText"
            Text="Нет плейлистов"
            Style="{StaticResource CaptionTextStyle}"
            HorizontalAlignment="Center"
            VerticalAlignment="Center"
            Visibility="Collapsed" />
    </Grid>
</Page>