<UserControl x:Class="VKZ.Views.SearchView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:converters="clr-namespace:VKZ.Converters"
             Background="#1A1B1E">

    <UserControl.Resources>
        <converters:SecondsToTimeConverter x:Key="SecondsToTime" />
    </UserControl.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="#232529" Padding="4" Margin="0,0,0,16">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <TextBox Name="SearchTextBox"
                         Grid.Column="0"
                         Text=""
                         FontSize="16"
                         Padding="12,8"
                         Background="Transparent"
                         Foreground="White"
                         KeyDown="OnSearchKeyDown" />

                <Button Grid.Column="1"
                        Content="🔍"
                        FontSize="16"
                        Background="#0077FF"
                        Foreground="White"
                        Padding="12,8"
                        Margin="4,0"
                        Click="OnSearchClick" />
            </Grid>
        </Border>

        <ScrollViewer Grid.Row="1"
                      VerticalScrollBarVisibility="Auto"
                      HorizontalScrollBarVisibility="Disabled">
            <ItemsControl Name="SearchResultsList">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Background="#232529" Padding="10,8" Margin="0,2" Cursor="Hand">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>

                                <Border Grid.Column="0" Width="44" Height="44" Background="#2C2E33" Margin="0,0,10,0">
                                    <TextBlock Text="🎵" FontSize="22" HorizontalAlignment="Center" VerticalAlignment="Center" />
                                </Border>

                                <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                    <TextBlock Text="{Binding Title}" FontSize="14" FontWeight="SemiBold" Foreground="White" TextTrimming="CharacterEllipsis" />
                                    <TextBlock Text="{Binding Artist}" FontSize="12" Foreground="#8A8E99" TextTrimming="CharacterEllipsis" />
                                </StackPanel>

                                <TextBlock Grid.Column="2" Text="{Binding Duration, Converter={StaticResource SecondsToTime}}" FontSize="12" Foreground="#5C5F66" VerticalAlignment="Center" />
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </Grid>
</UserControl>